import * as React from 'React';

import './index.scss';

const Header: React.FC = () => {
  return (
    <header>
      <div className="container">
        <p>header</p>
      </div>
    </header>
  );
};

export default Header;
